# b2sky-gateway

## VM Options

```
-DapplicationName=b2sky-gateway -Dversion=1.0.0 -Denv=dev -Dbrand=b2sky -Dlog.level=INFO -Dlog.toStdout=true -Dlog.stdout.level=INFO -Dlog.format=json -Dlog.folder=./logs -Dlog.fileName=app.log -Dlog.type=async -Dlog.appender=file -Dlog.async.maxFlushTime=180000
```
